@model IEnumerable<LindaniDrivingSchool.Models.TimeSlots>

@{
    ViewBag.Title = "Index";
}

<h2>Time Slots</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SlotTime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SlotTime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TimeSlotId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TimeSlotId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TimeSlotId })
        </td>
    </tr>
}

</table>
